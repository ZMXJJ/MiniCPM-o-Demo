[pytest]
pythonpath = .
testpaths = minicpm_memory/tests
python_files = test_*.py
addopts = -q
filterwarnings =
    ignore::DeprecationWarning:pkg_resources
    ignore:.*pkg_resources.*:DeprecationWarning
markers =
    smoke: lightweight smoke tests (no model, no network)
